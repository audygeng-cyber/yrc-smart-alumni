{
  "name": "yrc-smart-alumni",
  "private": true,
  "workspaces": ["frontend", "backend"],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "concurrently -n web,api -c cyan,magenta \"npm run dev -w frontend\" \"npm run dev -w backend\"",
    "build": "npm run build -w frontend && npm run build -w backend",
    "lint": "npm run lint -w frontend && npm run lint -w backend",
    "test": "npm run test -w backend",
    "ci": "npm run build && npm run lint && npm run test",
    "ci:full": "npm run ci && docker build -t yrc-api:ci .",
    "gh:actions": "node scripts/github-actions-url.mjs",
    "verify:deploy": "node scripts/verify-deployment.mjs",
    "verify:deploy:deep": "node scripts/verify-deployment.mjs --deep",
    "verify:admin": "node scripts/verify-admin.mjs",
    "verify:line": "node scripts/verify-line-config.mjs",
    "migrations:list": "node scripts/list-supabase-migrations.mjs",
    "line:show-env": "node scripts/show-cloud-run-line-env.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
