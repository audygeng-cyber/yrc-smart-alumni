# Dependencies
node_modules/
# Workspaces: ใช้แค่ package-lock.json ที่ราก — อย่า commit lock ซ้อนใน frontend
frontend/package-lock.json

# Environment & secrets
.env
.env.local
.env.*.local
backend/.env
frontend/.env
*.pem

# git-secret (https://git-secret.io) — อย่า commit seed; ไฟล์ *.secret คือ ciphertext ที่อนุญาตให้ track
.gitsecret/keys/random_seed

# Build outputs
dist/
build/
.next/
out/

# Logs & OS
*.log
.DS_Store
Thumbs.db

# Temporary clone when syncing Cursor community skills (scripts/sync-cursor-community-skills.ps1)
_skill-vendor/

# Editor
.idea/
.vscode/*
!.vscode/extensions.json

# Data files (do not commit member PII)
*.xlsx
*.xls
*.csv
!**/fixtures/*.csv

# Supabase local (if used)
.branches/
.temp/
